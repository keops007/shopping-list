services:
  db:
    image: postgres:16
    container_name: shoplist_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shoplist
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    container_name: shoplist_api
    restart: always
    ports:
      - "8082:8081"
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: shoplist
      DB_PORT: 5432
      JWT_SECRET: 456546b5565465465465465
    volumes:
      - uploads:/app/uploads

volumes:
  pgdata:
  uploads:
